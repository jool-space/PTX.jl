id = "NVVM-SIDEFX-001"
status = "resolved"
severity = "high"
category = "lowering"
summary = "NVVM records marked both sideeffects and nomem still receive readnone or memory(none), allowing unrecognized void intrinsics to be deleted by older LLVM optimizers."
source_handoffs = ["round1-ci-pr24.md"]
validation_tier = "closed-world intrinsic-property inventory and optimized LLVM retention across supported Julia/LLVM versions"

[resolution]
pr = 26
commit = "e4dea7aab2539f850670b9ec12b14eb6a7f66ef9"
scope = "full"
evidence = [
  "independent closed-world inventory pins all eight IntrNoMem plus IntrHasSideEffects NVVM records",
  "optimized host LLVM retains exactly one call to every affected intrinsic instead of merely retaining declarations",
  "pure no-memory arithmetic control retains readnone on LLVM 15 and memory(none) on LLVM 16 and 18",
  "host/nvvm passes on Julia 1.10, 1.11, and 1.12 across LLVM 15, 16, and 18",
  "complete Julia 1.12 host suite passes 33778/33778",
]
