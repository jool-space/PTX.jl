id = "ADDRESS-001"
status = "resolved"
severity = "high"
category = "correctness"
summary = "Transpilation loses address-operand grouping when an address becomes an integer carrier, emitting a bare register instead of brackets."
source_handoffs = ["wave4-data-movement.md", "wave6-frontend-tests.md", "targeted-probes.md"]
validation_tier = "host roundtrip; optimized LLVM; ptxas; Ada runtime"

[resolution]
pr = 31
commit = "e634c675189df0b7a42bc56dcf6a081a93d03b73"
scope = "full"
evidence = [
  "public isbits Address marker covers 32-bit and 64-bit integer carriers while Core.LLVMPtr remains identity-preserving for exact pointer dispatch",
  "transpilation preserves simple bracketed addresses through declared aliases and byte offsets, and reviewed schemas bracket each integer payload exactly once",
  "structured vector, matrix, tensor, fabric, b128, and tcgen05 misses fail loud instead of entering scalar ABI inference",
  "801 host assertions plus combined 11069-assertion address, mbarrier, wrapper, fallback, and external-corpus validation cover direct, raw, lowering, and transpiler paths",
  "208 address-specific offline LLVM and ptxas probes pass across baseline and Blackwell targets, and an Ada CC 8.9 integer-address load round-trip passes at runtime",
]
