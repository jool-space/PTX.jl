id = "CLC-TRY-001"
status = "resolved"
severity = "high"
category = "correctness"
summary = "clusterlaunchcontrol.try_cancel emits both mandatory address operands without bracket grouping."
source_handoffs = ["wave1-lowering.md"]
validation_tier = "host assembly shape; optimized LLVM; ptxas"

[resolution]
pr = 31
commit = "e634c675189df0b7a42bc56dcf6a081a93d03b73"
scope = "full"
evidence = [
  "closed PTX 9.3 schema covers exactly the four generic/shared-CTA by base/multicast clusterlaunchcontrol.try_cancel forms",
  "both mandatory operands retain address roles and exact state-space validation through direct, raw, lowering, and transpiler paths",
  "wrong carriers, wrong state spaces, omitted operands, and unreviewed spellings fail loud rather than emitting scalar or destination-bearing fallbacks",
  "offline LLVM and ptxas probes assemble base and multicast forms at their reviewed sm_100, architecture-specific, and family-specific targets",
]
